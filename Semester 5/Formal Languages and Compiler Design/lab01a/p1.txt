start
puts("Give me a natural number");
int input_number = to_i(cuts(gets()));
bool is_prime = true;

for number in 2..input_number/2 do
	if !(input_number % number) do
		is_prime = false;
		break;
	end
end

puts(is_prime);
stop
